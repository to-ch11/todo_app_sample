<p style="color: green"><%= notice %></p>

<h1>タスク一覧</h1>

<div id="tasks">
  <% @tasks.each do |task| %>
    <%= render task %>
    <p>
      <%= link_to "タスクを見る", task %>
    </p>
  <% end %>
</div>

<%= link_to "＋新しいタスクを追加", new_task_path %>